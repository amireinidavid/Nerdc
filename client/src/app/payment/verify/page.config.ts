// This configuration prevents static generation for this specific page
export const dynamic = 'force-dynamic'
export const dynamicParams = true

// Ensures the page is never statically optimized
export const revalidate = 0 